package InterfaceExamples;

public interface Webdriver {
//if I craet any method with body it will throw error because interface is bydefauly abstract method
	public void click();//without body it wont give error because3 it is bydefault abstract
	//we canot creat the concreat class in interface but in abstract class we can creat concreatclass.
}
