package Inheritance;

public class InheritanceExample {
	
	/*
	 * types of inheritance
	 * Single
	 * Multiple
	 * Multilevel
	 * Hybrid
	 * Hierarchical
	 * Inheritance are applied at 2 level either at class level or at interface level
	 * 
	 * Class- class can inherit a class and we use the key word as extends
	 * interface can inherit a interface and we use the key word as extends
	 * When a class use a interface we have to use implements
	 * and interface can't inherit the class it is not possible
	 * whenever type of parent and child is same we use extends and whenever type is different we use extends
	 * 
	 * what all types are possible as class level
	 * single inheritance
	 * multilevel
	 * Hierarchical
	 * 
	 * Multipul is possible at interface and all above 3(single inheritance
	 * multilevel
	 * Hierarchical)are also at interface level
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

}
