package Package1;

 class ClassB {//remove public from ClassB and it will be treated as default access modifies571`
	 //all modifiers can be accessible in it class and in same package
	 //all modifiers can be accessible outside the class but in same package accept private modifier
// outside the package only public is accessible and  other modifiers are accessible within same package but outside class 
}
